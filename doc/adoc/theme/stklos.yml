extends: default

footer:
  recto:
    right:
      content: ' {chapter-title} | *{page-number}*'
  verso:
    left:
      content: '*{page-number}* | {chapter-title}'

role:
  rmargin:
    font_size: 0.8em
    font_color: '#777777'
    min_height_after: $base_line_height_length * 2

heading_h2:
  margin_bottom: 8 * $vertical_rhythm

caption:
  align: center
